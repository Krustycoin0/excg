name: excg
description: App di scambio crittovalute con Li.Fi
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  web3dart: ^3.0.0
  http: ^1.1.0
  lifi_flutter: ^0.2.2
  provider: ^6.0.5
  walletconnect_flutter: ^2.0.0
  qr_code_scanner: ^1.0.1
  shared_preferences: ^2.2.2

flutter:
  uses-material-design: true
  assets:
    - assets/