name: excg
description: App di scambio crittovalute
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  web3dart: ^3.0.0
  http: ^1.1.0
  lifi_flutter: ^0.2.1
  provider: ^6.0.5
  web3: ^0.3.0

flutter:
  uses-material-design: true
  assets:
    - assets/